package com.quizsystem.models;

public enum Verdict {
    PASS, // Indicates the student has passed
    FAIL, // Indicates the student has failed
    TBD   // Indicates the verdict is to be determined (usually during revisions)
}

